---
# infer.md — this intent's inference criterion (the BODY below, rendered into
# the agent's Intent Catalog every turn) plus optional frontmatter flags.
# Allowed key: clarify: <bool>. The intent id IS this directory's name; the
# sibling prompt.md is inlined while this intent is active, and hooks.yaml
# declares the completion contract.
---
Produce or revise the weekly operations report document.

enable-pre-post-scripts=true
public-hoist-pattern[]=@types/*
# pnpm < 11 silently ignores pnpm-11-only workspace keys (allowBuilds, hoisted
# overrides), which skips native postinstalls and surfaces as runtime ENOENT.
# Fail loudly instead (enforces engines.node and engines.pnpm).
engine-strict=true

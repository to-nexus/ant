rembg[cpu]>=2.0.57
fastapi>=0.115.0
uvicorn>=0.34.0
# uvicorn only asks for h11>=0.8 (no upper bound) and plain uvicorn parses
# HTTP/1.1 with h11, so the floor is pinned here: <0.16.0 accepts malformed
# Chunked-Encoding bodies (CVE-2025-43859, request smuggling).
h11>=0.16.0
pillow>=10.0.0
python-multipart==0.0.32

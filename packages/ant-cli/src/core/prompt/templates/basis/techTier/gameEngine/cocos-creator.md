## Game Engine: Cocos Creator (Phase 4+ stub)

Cross-platform 2D engine. Phase 3 leaves this as a stub — Phaser is the only engine with a non-stub body in Phase 3. Code emitted with `gameEngine === 'cocos-creator'` falls back to the universal `domain === 'game'` overlay and MUST flag the missing engine partial as an open question.

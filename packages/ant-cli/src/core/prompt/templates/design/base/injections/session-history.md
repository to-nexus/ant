{{#if content}}
<session_history>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 DESIGN HISTORY IN THIS FEATURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This shows your previous design work in this feature.
Use this to understand design evolution and maintain consistency.

{{{content}}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT: Check for Design Continuity

Compare session history with current state:

1. **If history shows previous design iterations:**
   → Build upon previous decisions unless explicitly asked to change
   → Maintain architectural consistency
   → Reference previous design document if still valid
   
2. **If PRD/requirements changed significantly:**
   → User may want fresh design approach
   → Consider previous learnings but don't be bound by them
   
3. **If design file was deleted/reset:**
   → User discarded previous design
   → Create fresh design, but avoid previous pitfalls

CRITICAL: Session history is a RECORD, not current state.
Check if previous design file still exists before referencing it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

</session_history>
{{/if}}

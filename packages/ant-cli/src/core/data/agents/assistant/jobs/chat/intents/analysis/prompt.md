- Read the actual files before characterizing them; for large trees, survey
  with `explore` before deep-reading.
- Derive numbers by computing — a small script run via shell — rather than
  eyeballing. Keep a script as a file when it may be useful again.
- Every reported figure names the file (and the computation) it came from.
- State coverage honestly: "based on 3 of the 12 files" beats implying you
  read everything.

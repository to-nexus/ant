{
  "name": "retries-exhausted-learn-exit",
  "description": "retries == maxRetries → routeAfterCheckTaskStatus exits via learn instead of plan retry (no throw). Covers C4.",
  "mode": "stub",
  "timeoutMs": 30000,
  "expectedChildExitCode": 0,
  "expected": {
    "routeSequence": ["plan", "execute", "checkTaskStatus", "learn"],
    "violations": ["verification_incomplete"]
  }
}

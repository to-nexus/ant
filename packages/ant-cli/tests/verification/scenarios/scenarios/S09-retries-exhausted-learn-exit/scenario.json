{
  "name": "retries-exhausted-learn-exit",
  "description": "retries == maxRetries → routeAfterCheckTaskStatus exits via learn instead of enforce → learn path (no plan throw). Covers C4.",
  "mode": "stub",
  "timeoutMs": 30000,
  "expectedChildExitCode": 0,
  "expected": {
    "routeSequence": ["plan", "execute", "checkTaskStatus", "learn"],
    "violations": ["verification_incomplete"]
  }
}

{
  "name": "repeated-plan-hash-fan-out",
  "description": "Overlay mode: plan returns the same JSON twice. Under always-fan-out the second cycle still fans out to per-target error sub-tasks regardless of plan-hash repetition (the legacy forceByRepeat machinery was removed). Covers C7.",
  "mode": "overlay",
  "timeoutMs": 30000,
  "expectedChildExitCode": "any",
  "expected": {
    "routeSequence": ["plan", "*", "plan"],
    "flagSet": ["_batchSplitRequeued"]
  }
}

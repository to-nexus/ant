# Self-hosting

This page has been split by server mode:

- **Local self-host** (single laptop, no OAuth, Redis in Docker):
  [../local-mode/install.md](../local-mode/install.md)
- **Cloud self-host** (single VM with OAuth, or multi-tenant
  Kubernetes): [../cloud-mode/install.md#self-host-cloud-personac](../cloud-mode/install.md#self-host-cloud-personac)

The runtime is identical across both; pick by your operator constraints
(OAuth, multi-user, IDE pod orchestration).
